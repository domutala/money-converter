name: Deploy
on:
  push:
    branches:
      - dev
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    env:
      BRANCHE_NAME: ${{ github.ref_name}}

    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo ${{ env.BRANCHE_NAME }}

            # cd ${{ secrets.PROJECT_FOLDER}}/dev/webapp
            # yarn

            # cd ${{ secrets.PROJECT_FOLDER}}/dev/api
            # yarn

            # sudo systemctl restart money-manager.api.${{env.BRANCHE_NAME}}.service
            # sudo systemctl restart money-manager.webapp.${{env.BRANCHE_NAME}}.service
